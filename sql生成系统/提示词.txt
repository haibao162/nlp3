员工明细表插入数据，员工名称，业务名称，创建时间，帮我生成一条sql。员工明细表detail，字段信息：name：员工名称，bussiness：业务名称。

帮我查一下员工明细表有哪些字段，生成对应的sql语句？员工明细表名为detail，字段如下：name：员工名称，bussiness：业务名称

帮我查询没有签约的用户名称，生成对应的sql语句。
数据库信息如下：表SignTable字段信息：isSign：是否签约，1：签约，0：未签约，id：用户id。表SignTable和表Username关联字段有：SignTable.id=Username.userId，表Username字段信息：userId: 用户id，name：用户名称。


[{1,2,{1.id=2.id}},{1,3, 1.no=3.order_no}]
